# Git plugin

## Purpose

Git workflow for the repo: commit staged files, craft message, push. Provides the git-commit-staged-files command (slash command in Cursor; available in Claude when using Colada plugins).
