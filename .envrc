# Optional: load local env so ENABLE_LOCAL_AGENT_* are set for postinstall
# What this file does: see .envrc.agents.md
# Load order: .env first, then .envrc.local so .envrc.local overrides .env (local overrides base)
dotenv
source_env_if_exists .envrc.local
