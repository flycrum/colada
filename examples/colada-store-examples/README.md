# colada-store examples

Minimal Vite + Vue 3 app that imports `defineColadaStore` from `@colada/colada-store` and uses the monorepo’s shared ESLint, Prettier, and TypeScript configs.

- **Run dev:** from repo root `pnpm dev`, or from this folder `pnpm dev`
- **Build:** from repo root `pnpm build` (builds colada-store first, then this app)
