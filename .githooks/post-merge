#!/bin/sh
# Runs after successful git merge (e.g. after git pull).
# Syncs Claude local marketplace/settings and Cursor commands/rules per ENABLE_LOCAL_AGENT_* in .envrc.local / .env.
# CWD is repo root when the hook runs.
node .agents/plugins/agents-plugin-marketplaces/scripts/marketplace-claude-sync.js
node .agents/plugins/agents-plugin-marketplaces/scripts/marketplace-cursor-sync.js
