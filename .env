# Local agent marketplace toggles (see .agents/plugins/env-variables)
# Committed defaults; override in .envrc.local (gitignored) if needed
ENABLE_LOCAL_AGENT_CLAUDE=false
ENABLE_LOCAL_AGENT_CURSOR=false
